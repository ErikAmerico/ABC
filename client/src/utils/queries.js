// Job.find({}).populate('origin.company origin.contact origin.altContact destination.company destination.contact destination.altContact').exec((err, jobs) => {
//     // Now, the origin and destination company and contacts fields in the jobs are fully populated.
//   });
